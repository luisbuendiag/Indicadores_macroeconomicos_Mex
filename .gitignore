# Entorno / secretos
.env
*.env
!.env.example

# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
.ruff_cache/

# Respaldos rotados del pipeline (se conserva last_valid.json)
data/_backup/
data/_candidate.json

# Caché de fuentes de calendario (se regenera en CI o se descarga online)
data/cache/

# Descargas de fuentes (XLSX/CSV) obtenidas en runtime por los conectores
data/downloads/

# Sistema
.DS_Store
Thumbs.db
